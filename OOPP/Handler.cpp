#include "Handler.h"
/*
Handler::Handler(){
}

void Handler::Update() {
	//auto l_front = object.begin();
	for (int i = 0; i < Enemies.size(); i++) {
		auto l_front = Enemies.begin();
		std::advance(Enemies, i);
		GameObject* tempObject = *l_front;
		tempObject->Update();
	}

}

void Handler::Render() {
	for (int i = 0; i < Enemies.size(); i++) {
		auto l_front = Enemies.begin();
		std::advance(Enemies, i);
		GameObject* tempObject = *l_front;
		tempObject->Render();
	}
}

void Handler::addObject(ID id, void* object) {
	//this->object.push_back(object);
	switch (id) {
		case Enemy:
			this->Enemies.push_back(new GameObject (object));
	}

}

void Handler::removeObject(ID id, void* object) {
	this->object.remove(object);
}

*/