#include "Handler.h"
/*
Handler::Handler(){
}

void Handler::Update() {
	//auto l_front = object.begin();
	for (int i = 0; i < object.size(); i++) {
		auto l_front = object.begin();
		std::advance(object, i);
		GameObject& tempObject = *l_front;
		tempObject.Update();
	}

}

void Handler::Render() {
	for (int i = 0; i < object.size(); i++) {
		auto l_front = object.begin();
		std::advance(object, i);
		GameObject& tempObject = *l_front;
		tempObject.Render();
	}
}

void Handler::addObject(Player object) {
	this->object.push_back(object);
}

void Handler::removeObject(Player object) {
	this->object.remove(object);
}

*/